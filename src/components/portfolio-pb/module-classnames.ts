import { ModuleClassnamesParams } from '@divi/module';
import { PortfolioPBAttrs } from './types';

/**
 * Module classnames function for Portfolio PB Module.
 */
export const moduleClassnames = ({
  classnamesInstance,
}: ModuleClassnamesParams<PortfolioPBAttrs>): void => {
  // The module gets its base class name from metadata.
};
